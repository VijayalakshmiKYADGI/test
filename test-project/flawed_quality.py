def process_users(): 
    users = []  
    for user in users_list:  
        user_name=user.name.lower() 
        if len(user_name)>5:import os 
        users.append(user_name)
    return users 

def messy_function(x,y): 
    z=x+y
    print(z) 
    return z

class bad_class: 
    def __init__(self):
        self.x 
